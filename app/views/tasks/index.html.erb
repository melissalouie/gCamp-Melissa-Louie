<div class="container">
  <div class="page-header">
    <div class="pull-right">
      <%= link_to 'All', tasks_path %>&nbsp;&nbsp;|
      &nbsp;<%= link_to 'Incomplete', '/incomplete' %>&nbsp;&nbsp;
      <%= link_to 'New Task', new_project_task_path(@project), class: 'btn btn-info' %>
    </div>
    <h1>Tasks</h1>
  </div>
  <p id="notice"><%= notice %></p>
  <ol class="breadcrumb">
    <li><%= link_to 'Projects', projects_path %></li>
    <li><%= link_to @project.name, project_path(@project) %></li>
    <li>Tasks</li>
  </ol>

  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th class="col-md-6">Description</th>
        <th class="col-md-2">Due Date</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @tasks.each do |task| %>
        <tr>
          <td class="col-md-6"><%= link_to task.description, project_task_path(@project, task) %></td>
          <td class="col-md-2"><%= task.due_date %></td>
          <td class="text-right"><%= link_to 'View', project_task_path(@project, task), class: 'btn btn-success' %>
                                 <%= link_to 'Edit', edit_project_task_path(@project, task), class: 'btn btn-info' %>
                                 <%= link_to 'Delete', project_task_path(@project, task), method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
