<div class="container">
  <div class="page-header">
      <%= link_to 'Delete', task_path(@task), method: :delete, class: 'btn btn-danger pull-right' %>
      <%= link_to 'Edit', edit_task_path(@task), class: 'btn btn-info pull-right', id: 'edit-button' %>
    <h1><%= @task.description %> |  Due Date: <%= @task.due_date %></h1>
  </div>
    <ol class="breadcrumb">
      <li><%= link_to 'Tasks', tasks_path %></li>
      <li class="active"><%= @task.description %></li>
    </ol>
</div>
